{
  "name": "mise",
  "version": "0.1.0",
  "private": true,
  "description": "Mise backend — Postgres + per-request client isolation, auth layer, and pgvector RAG knowledge base.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "migrate": "node scripts/migrate.js",
    "seed": "node scripts/seed.js",
    "ingest:legislation": "node scripts/ingest-legislation.js",
    "test": "node --test 'test/**/*.test.js'"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.97.1",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.21.0"
  }
}
