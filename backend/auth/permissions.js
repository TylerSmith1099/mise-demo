// Auth Agent File Save Protocol — canonical implementation lives at src/permissions.js.
// Role permission map, requireTier(), requireAccess(), canAccess(), MVP_ROLE_TIERS.
export {
  MVP_ROLE_TIERS,
  PERMISSION_MAP,
  canAccess,
  requireTier,
  requireAccess,
} from '../src/permissions.js';
